# 安装与使用

## 安装

1. 在浏览器安装 Tampermonkey。
2. 打开 `dist/BilibiliPrecisionSearch.user.js`，复制全部内容。
3. 在 Tampermonkey 中新建脚本，覆盖编辑器内容并保存。
4. 打开或刷新任意 B站搜索页，例如 `https://search.bilibili.com/all?keyword=费德勒`。

建议使用 Chrome / Edge 111+ 或 Firefox 113+。

页面搜索分类附近会出现“🎯 精准搜索”；如果页面标签暂时未加载，入口会显示在右上角（窄屏为右下角圆形按钮）。升级旧版时，直接用 v1.2.0 文件覆盖原脚本即可。

## 使用

1. 保持原搜索框中的关键词，点击“🎯 精准搜索”。
2. 搜索完成后，在“长期质量 / 增长趋势 / 最新热播”中切换排序；三个视图共用候选池，不会重复请求整轮搜索。
3. 查看每张结果卡的“相关、质量、增长、时效”和状态标签；展开说明可查看命中、扣分、数据完整度及在线采样情况。
4. 点击“返回原生”或按 `Esc`，即可回到未被改动的 B站原生结果。

可用查询写法：

```text
费德勒 2017 澳网 纳达尔
"Wilson Pro Staff 97" v14
RTX 5090 -笔记本
```

- 双引号：要求完整短语精确命中。
- `-词语`：候选命中排除词时直接过滤。
- 年份、数字、型号和版本：必须在标题、标签或简介中精确命中。

## 三个排序怎样选

- 想找经得住时间检验、累计收藏和深度互动更强的视频，选“长期质量”。发布时间不参与这个分数。
- 想找正在加速增长的视频，选“增长趋势”。第一次搜索某个视频时只能估算；至少间隔约 30 分钟再次搜索后，才可能出现“真实增速”。
- 想看最近发布且此刻有人看的内容，选“最新热播”。当前在线人数是瞬时近似值，多 P 视频默认采样第一 P。

如果统计或在线接口暂时不可用，脚本会按已有字段降级并显示完整度，不会把缺失值当成零。

若提示“B站触发了访问验证”，脚本已自动停止请求。等待一段时间后重试；原生搜索始终可用。
