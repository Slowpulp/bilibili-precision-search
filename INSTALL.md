# 安装与使用

## 安装

1. 在浏览器安装 Tampermonkey。
2. 打开 `dist/BilibiliPrecisionSearch.user.js`，复制全部内容。
3. 在 Tampermonkey 中新建脚本，覆盖编辑器内容并保存。
4. 打开或刷新任意 B站搜索页，例如 `https://search.bilibili.com/all?keyword=费德勒`。

建议使用 Chrome / Edge 111+ 或 Firefox 113+。

页面搜索分类附近会出现“🎯 精准搜索”；如果页面标签暂时未加载，入口会显示在右上角（窄屏为右下角圆形按钮）。

## 使用

1. 保持原搜索框中的关键词，点击“🎯 精准搜索”。
2. 在“严格 / 标准 / 探索”中选择模式；默认使用“标准”。
3. 查看每张结果卡的“相关”“质量”和命中词；展开“查看匹配与质量说明”可看完整解释。
4. 点击“返回原生”或按 `Esc`，即可回到未被改动的 B站原生结果。

可用查询写法：

```text
费德勒 2017 澳网 纳达尔
"Wilson Pro Staff 97" v14
RTX 5090 -笔记本
```

- 双引号：要求短语精确命中；严格模式要求出现在标题或标签。
- `-词语`：只要候选命中该排除词就会被过滤。
- 年份、数字、型号：必须在标题、标签或简介中精确命中。

若提示“B站触发了访问验证”，脚本已自动停止请求。等待 30～120 秒后重试；原生搜索始终可用。
