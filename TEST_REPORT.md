# 验证报告

验证日期：2026-09-07

目标版本：1.2.0

## 自动化验证

发布验证命令：

```powershell
npm run verify
```

最终结果：语法检查、74 项自动化测试、单文件构建和构建产物独立语法检查全部通过，0 失败。

覆盖范围：

- userscript 元数据、版本号、站点匹配和匿名跨域授权。
- 搜索卡规范化、课程卡过滤、互动零占位转缺失、BV 号去重和五路来源合并。
- WBI key/签名、共享请求、TTL、取消、`-403` 单次刷新、结构性端点降级，以及风控立即停止。
- 详情统计补全、分 P 信息、`万/亿/+` 在线人数、`show_switch` 和 page-scope 在线语义。
- 统一相关性门槛、引号短语、排除词、数字/年份/型号精确命中和受限模糊匹配。
- 长期质量完全独立于发布时间与候选池；固定锚点、平滑互动率、缺失数据向中性收缩。
- 真实快照增长、低置信度生命周期估算、负向统计回退保护和本地快照限量/过期清理。
- 发布时间与在线热度时效分、查询意图半衰期，以及在线缺失保持中性而非获得额外奖励。
- 5 分相关性硬分档，以及质量 40/60、增长 35/55/10、时效 35/55/10 的档内排序。
- 三个排序共享一次召回、平衡详情 shortlist、在线 shortlist、缓存、竞态和旧结果防回显。
- Shadow DOM、原生节点保留与恢复、安全文本渲染、24 条分页、键盘标签操作、窄屏布局和降级提示。

构建产物：`dist/BilibiliPrecisionSearch.user.js`

```text
大小：153,383 字节
SHA-256：1340C4C0CDF4B3E245358226149C7BCB76D8292033E2B8302ADA09FF9F946CE1
```

## 真实 B站接口低频验证

在匿名、不带 Cookie 的条件下，对当前适配器做了一次节制的在线验证：

```text
https://api.bilibili.com/x/web-interface/nav
https://api.bilibili.com/x/web-interface/wbi/search/type
https://api.bilibili.com/x/web-interface/view
https://api.bilibili.com/x/player/online/total
```

样例查询为 `网球`，仅请求综合排序第一页、`page_size=5`，再对首条规范化视频验证详情与在线接口。结果：

- 搜索与 WBI 签名成功，规范化后得到 4 条视频候选。
- 首条候选 `BV1Na411U7j5` 的详情接口返回播放、点赞、收藏、评论、弹幕、投币和分享七项可解析累计统计。
- 分 P 信息可解析；在线接口返回可用人数，且适配器明确标记为 `page` 范围。
- 本次请求未触发访问验证。

## 未进行的测试

- 未以完整的 15 路候选、60 条详情和 24 条在线采样对真实接口做压力测试，避免主动触发 B站风控；高并发、部分失败与风控分支均使用模拟响应验证。
- 未替用户安装或覆盖 Tampermonkey 脚本，也未使用登录 Cookie 或账号状态。
- 未把真实浏览器截图级视觉核对列为通过项；页面结构和交互使用 JSDOM 自动化验证，安装后仍建议人工查看一次按钮位置和主题效果。
