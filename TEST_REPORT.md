# 验证报告

验证日期：2026-09-07

目标版本：1.0.0

## 自动化验证

发布命令：

```powershell
npm run verify
```

覆盖范围：

- userscript 元数据、版本号、站点匹配、跨域授权与构建语法。
- HTML 高亮清理、Unicode 归一化、中文长串回退、英文/数字/型号边界、引号短语、排除词和 MD5 已知向量。
- API 响应规范化、课程卡过滤、BV 号去重、多路来源合并、WBI key/签名、共享 nav、取消、`-403` 刷新和风控并发停止。
- 严格/标准/探索硬门槛、数字与版本限定词、无关高质量结果过滤、小样本质量收缩、往年查询时效处理、相关性分档排序。
- Shadow DOM 挂载、原生节点保留、筛选/结果区 inert 与精确恢复、安全文本渲染、24 条客户端分页、窄屏 FAB。
- SPA `pushState`、`vt` 参数忽略、整个 `#app` 替换后的重新发现、原生标签点击作用域、旧请求竞态与本地排除语法不污染上游召回。

最终结果：`npm run verify` 通过，44 项测试全部通过，0 失败；发布脚本通过独立语法检查，大小为 103,004 字节。

发布脚本 SHA-256：

```text
536FFA67EEB90B5FDE3C71166778B715BDCF8294DFB9797174CCBF4CE9B6F9E8
```

## 真实 B站接口验证

在匿名、不带 Cookie 的条件下使用当前适配器连接：

```text
https://api.bilibili.com/x/web-interface/nav
https://api.bilibili.com/x/web-interface/wbi/search/type
```

观察结果：

- WBI key、签名和 JSON 解析成功；`page_size=50` 可用。
- `totalrank / click / pubdate / stow / dm` 五种排序均返回正常视频结果。
- `search_type=video` 中的非视频课程卡可被过滤。
- 搜索响应对 `https://search.bilibili.com` 当前提供 CORS，脚本的匿名 GM 传输仍为主路径。

节制请求的端到端样例（4 路 × 1 页 × 20 条）：

| 查询 | 原始有效 | BV 去重后 | 严格保留 | 标准保留 | 探索保留 |
|---|---:|---:|---:|---:|---:|
| `费德勒 2017 澳网 纳达尔` | 80 | 70 | 2 | 10 | 10 |
| `Wilson Pro Staff 97 v14` | 80 | 44 | 6 | 8 | 8 |

型号边界修复后又对综合排序第一页做了复测：真实视频 `BV1xe411z7Ad` 的标题完整包含 `Wilson Pro Staff 97 V14`，三种模式均得到相关性与覆盖率 `1.0000`，全部词项精确命中并排第 1。

## 页面与兼容性验证

- 已核对当前 B站搜索页的 SSR格局：搜索页使用 Vue/Pinia 接管 `#app`；本脚本只向 `body` 增加一个 Shadow host，不向 Vue 管理的列表插入节点。
- JSDOM 页面模拟验证了打开/关闭、原生状态恢复、SPA 根节点替换、Shadow 安全渲染、分页和窄屏定位。
- 浏览器最低建议版本：Chrome / Edge 111+、Firefox 113+。
- 尝试在隔离的应用内浏览器中连接真实页面做截图级验证时，页面已加载，但自动化桥接连续超时，因此未把“真实浏览器视觉截图”列为通过项。该限制不影响真实接口验证和 DOM/运行时自动化测试，但安装后仍建议人工查看一次按钮位置和主题效果。

## 未进行的破坏性测试

- 未主动制造 B站真实 412/429 风控；相关分支使用模拟响应验证。
- 未替用户安装 Tampermonkey 脚本，也未使用登录 Cookie 或账号状态。
